# Feature modules

Each feature owns `api/`, `components/`, `hooks/`, `helpers/`, `types/`, and an exported public entry point. Cross-feature imports should use the public entry point, not internal files.
