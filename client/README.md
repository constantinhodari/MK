# Client application boundary

This directory is the production frontend boundary planned in Phase 1. New React features belong under `client/src/features/<domain>` and must use the shared contracts in `docs/`.

The current root Vite app remains a visual reference until the public shell is migrated in the implementation phase.
