# Server application boundary

This directory is the production Node.js/Express API boundary planned in Phase 1. HTTP routes stay thin; controllers orchestrate; services own business rules; models own persistence.
